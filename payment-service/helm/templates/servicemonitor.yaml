apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor

metadata:
  name: {{ .Chart.Name }}
  labels:
    release: monitoring

spec:
  namespaceSelector:
    matchNames:
      - {{ .Release.Namespace }}

  selector:
    matchLabels:
      app: {{ .Chart.Name }}

  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 15s