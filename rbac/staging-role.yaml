apiVersion: rbac.authorization.k8s.io/v1
kind: Role

metadata:
  namespace: staging
  name: staging-developer-role

rules:
- apiGroups: ["", "apps"]

  resources:
    - pods
    - services
    - deployments

  verbs:
    - get
    - list
    - watch
    - create
    - update
    - delete